# Database - PostgreSQL (Local Development)
# IMPORTANT: Update these values to match your local PostgreSQL setup
spring.datasource.url=jdbc:postgresql://aws-1-ap-southeast-2.pooler.supabase.com:6543/postgres?user=postgres.qautxdsvszupasntdnrr&password=00TI2hXgz5mRUMPp&prepareThreshold=0
spring.datasource.username=postgres
spring.datasource.password=00TI2hXgz5mRUMPp
spring.datasource.driver-class-name=org.postgresql.Driver


# Connection Pool Configuration
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
# JPA Configuration
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# JWT Configuration
jwt.secret=mySecretKeyForJWTTokenGenerationThatShouldBeAtLeast256BitsLong123456
jwt.expiration=86400000
# Logging
logging.level.org.springframework.security=DEBUG
logging.level.com.example.backendservice=DEBUG
# Swagger/OpenAPI
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.path=/swagger-ui.html
# CORS - Allow all for development
cors.allowed-origins=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000
# ============================================
# LOCAL POSTGRESQL SETUP:
# ============================================
# 1. Install PostgreSQL on your machine
# 2. Create database: CREATE DATABASE swd_dev;
# 3. Update username/password above if different
# 
# Or use Docker:
# docker run --name swd-postgres -e POSTGRES_DB=swd_dev -e POSTGRES_PASSWORD=postgres -p 5432:5432 -d postgres:15
# ============================================